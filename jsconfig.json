// jsconfig.paths.json or tsconfig.paths.json
{
    "compilerOptions": {
      "baseUrl": ".",
      "paths": {
        "public/*": ["public/src/*"],
        "@library/*": ["library/src/*"]
      }
    }
}